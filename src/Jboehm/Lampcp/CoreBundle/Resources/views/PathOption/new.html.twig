{% extends 'JboehmLampcpCoreBundle::layout.html.twig' %}

{% block content %}
    <h1>PathOption creation</h1>

    <form action="{{ path('config_pathoption_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('config_pathoption') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}