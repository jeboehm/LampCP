{% extends 'JboehmLampcpCoreBundle::layout.html.twig' %}

{% block content %}
    <h1>{{ "show.page.domain"|trans() }}</h1>

    <table class="record_properties">
        <tbody>
        <tr>
            <th>{{ "all.column.id"|trans() }}</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>{{ "domain.column.domain"|trans() }}</th>
            <td>{{ entity.domain }}</td>
        </tr>
        <tr>
            <th>{{ "domain.column.hasweb"|trans() }}</th>
            <td>{{ entity.hasWeb }}</td>
        </tr>
        <tr>
            <th>{{ "domain.column.hasmail"|trans() }}</th>
            <td>{{ entity.hasMail }}</td>
        </tr>
        <tr>
            <th>{{ "domain.column.hasssh"|trans() }}</th>
            <td>{{ entity.hasSSH }}</td>
        </tr>
        <tr>
            <th>{{ "domain.column.gid"|trans() }}</th>
            <td>{{ entity.gid }}</td>
        </tr>
        <tr>
            <th>{{ "domain.column.path"|trans() }}</th>
            <td>{{ entity.path }}</td>
        </tr>
        </tbody>
    </table>

    <ul class="record_actions">
        <li>
            <a href="{{ path('config_domain') }}">
                {{ "button.back"|trans() }}
            </a>
        </li>
        <li>
            <a href="{{ path('config_domain_edit', { 'id': entity.id }) }}">
                {{ "button.edit"|trans() }}
            </a>
        </li>
        <li>
            <form action="{{ path('config_domain_delete', { 'id': entity.id }) }}" method="post">
                {{ form_widget(delete_form) }}
                <button type="submit">{{ "button.delete"|trans() }}</button>
            </form>
        </li>
    </ul>
{% endblock %}