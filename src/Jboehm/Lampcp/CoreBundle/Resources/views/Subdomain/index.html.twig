<h1>Subdomain list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Subdomain</th>
            <th>Path</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('config_subdomain_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.subdomain }}</td>
            <td>{{ entity.path }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('config_subdomain_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('config_subdomain_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('config_subdomain_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
