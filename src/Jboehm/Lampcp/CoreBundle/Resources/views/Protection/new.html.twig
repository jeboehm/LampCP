{% extends 'JboehmLampcpCoreBundle::layout.html.twig' %}

{% block content %}
    <h1>Protection creation</h1>

    <form action="{{ path('config_protection_create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('config_protection') }}">
                Back to the list
            </a>
        </li>
    </ul>
{% endblock %}