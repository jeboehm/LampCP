{#
 # LampCP
 # https://github.com/jeboehm/LampCP
 #
 # Licensed under the GPL Version 2 license
 # http://www.gnu.org/licenses/gpl-2.0.txt
 #
 #}

{% extends 'JeboehmLampcpCoreBundle:Base:layout.html.twig' %}

{% block content %}
    <h1>{{ "title.page.protectionuser"|trans() }}</h1>

    <table class="table table-striped table-bordered">
        <thead>
        <tr>
            <th>{{ "all.column.id"|trans() }}</th>
            <th>{{ "jeboehm.lampcp.corebundle.protectionusertype.username"|trans() }}</th>
            <th>{{ "all.column.actions"|trans() }}</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('config_protectionuser_show', { 'entity': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.username }}</td>
                <td>
                    <div class="btn-group">
                        <a class="btn btn-info btn-mini"
                           href="{{ path('config_protectionuser_show', { 'entity': entity.id }) }}">{{ "button.show"|trans() }}</a>
                        <a class="btn btn-warning btn-mini"
                           href="{{ path('config_protectionuser_edit', { 'entity': entity.id }) }}">{{ "button.edit"|trans() }}</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('config_protectionuser_new', { 'protection': protection.id }) }}">
        {{ "button.create"|trans() }}
    </a>

    <a class="btn"
       href="{{ path('config_protection_show', { 'entity': protection.id }) }}">{{ "show.page.protection"|trans() }}</a>
{% endblock %}
