{#
 # LampCP
 # https://github.com/jeboehm/LampCP
 #
 # Licensed under the GPL Version 2 license
 # http://www.gnu.org/licenses/gpl-2.0.txt
 #
 #}

{% extends 'JeboehmLampcpCoreBundle:Base:layout.html.twig' %}

{% block content %}
    <h1>{{ "edit.page.protectionuser"|trans() }}</h1>

    <form action="{{ path('config_protectionuser_update', { 'id': entity.id }) }}"
          method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}

        {{ macros.formbuttongrp(path('config_protectionuser', { 'protectionid': entity.protection.id }), path('config_protectionuser_delete', { 'id': entity.id })) }}
    </form>
{% endblock %}
