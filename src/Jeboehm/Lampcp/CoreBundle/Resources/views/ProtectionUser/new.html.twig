{#
 # LampCP
 # https://github.com/jeboehm/LampCP
 #
 # Licensed under the GPL Version 2 license
 # http://www.gnu.org/licenses/gpl-2.0.txt
 #
 #}

{% extends 'JeboehmLampcpCoreBundle:Base:layout.html.twig' %}

{% block content %}
    <form action="{{ path('config_protectionuser_create', { 'protection': protection.id }) }}"
          class="form-horizontal" method="post" autocomplete="off" {{ form_enctype(form) }}>

        {{ form_row(form.username, {'form_type': 'horizontal'}) }}
        {{ form_row(form.password.first, {'form_type': 'horizontal'}) }}
        {{ form_row(form.password.second, {'form_type': 'horizontal'}) }}

        {{ form_rest(form) }}

        {{ macros.formbuttongrp(path('config_protectionuser', { 'protection': entity.protection.id })) }}
    </form>
{% endblock %}

{% block title %}
    {{ "create.page.protectionuser"|trans() }}
{% endblock %}
