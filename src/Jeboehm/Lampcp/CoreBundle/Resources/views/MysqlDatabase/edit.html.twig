{#
 # LampCP
 # https://github.com/jeboehm/LampCP
 #
 # Licensed under the GPL Version 2 license
 # http://www.gnu.org/licenses/gpl-2.0.txt
 #
 #}

{% extends 'JeboehmLampcpCoreBundle:Base:layout.html.twig' %}

{% block content %}
    <h1>{{ "edit.page.mysqldatabase"|trans() }}</h1>

    <form action="{{ path('config_mysqldatabase_update', { 'id': entity.id }) }}"
          class="form-horizontal" method="post" {{ form_enctype(edit_form) }}>

        {{ form_row(edit_form.name, {'form_type': 'horizontal'}) }}
        {{ form_row(edit_form.comment, {'form_type': 'horizontal'}) }}
        {{ form_row(edit_form.password.first, {'form_type': 'horizontal'}) }}
        {{ form_row(edit_form.password.second, {'form_type': 'horizontal'}) }}

        {{ form_rest(edit_form) }}

        {{ macros.formbuttongrp(path('config_mysqldatabase'), path('config_mysqldatabase_delete', { 'id': entity.id })) }}
    </form>
{% endblock %}
