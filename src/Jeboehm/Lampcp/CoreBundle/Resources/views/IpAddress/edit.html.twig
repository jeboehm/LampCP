{#
 # LampCP
 # https://github.com/jeboehm/LampCP
 #
 # Licensed under the GPL Version 2 license
 # http://www.gnu.org/licenses/gpl-2.0.txt
 #
 #}

{% extends 'JeboehmLampcpCoreBundle:Base:layout.html.twig' %}

{% block content %}
    <h1>{{ "edit.page.ipaddress"|trans() }}</h1>

    <form action="{{ path('config_ipaddress_update', { 'id': entity.id }) }}"
          method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}

        {{ macros.formbuttongrp(path('config_ipaddress'), path('config_ipaddress_delete', { 'id': entity.id })) }}
    </form>
{% endblock %}
