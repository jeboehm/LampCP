{#
 # LampCP
 # https://github.com/jeboehm/LampCP
 #
 # Licensed under the GPL Version 2 license
 # http://www.gnu.org/licenses/gpl-2.0.txt
 #
 #}

{% extends 'JeboehmLampcpCoreBundle::layout.html.twig' %}

{% block content %}
    <h1>{{ "title.page.ipaddress"|trans() }}</h1>

    <table class="table table-striped table-bordered">
        <thead>
        <tr>
            <th>{{ "all.column.id"|trans() }}</th>
            <th>{{ "jeboehm.lampcp.corebundle.ipaddresstype.alias"|trans() }}</th>
            <th>{{ "jeboehm.lampcp.corebundle.ipaddresstype.ip"|trans() }}</th>
            <th>{{ "jeboehm.lampcp.corebundle.ipaddresstype.port"|trans() }}</th>
            <th>{{ "jeboehm.lampcp.corebundle.ipaddresstype.hasSsl"|trans() }}</th>
            <th>{{ "jeboehm.lampcp.corebundle.ipaddresstype.ipv6"|trans() }}</th>
            <th>{{ "all.column.actions"|trans() }}</th>
        </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('config_ipaddress_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.alias }}</td>
                <td>{{ entity.ip }}</td>
                <td>{{ entity.port }}</td>
                <td>{{ entity.hasSsl }}</td>
                <td>{{ entity.ipv6 }}</td>
                <td>
                    <div class="btn-group">
                        <a class="btn btn-info btn-mini"
                           href="{{ path('config_ipaddress_show', { 'id': entity.id }) }}">{{ "button.show"|trans() }}</a>
                        <a class="btn btn-warning btn-mini"
                           href="{{ path('config_ipaddress_edit', { 'id': entity.id }) }}">{{ "button.edit"|trans() }}</a>
                    </div>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="btn btn-primary" href="{{ path('config_ipaddress_new') }}">
        {{ "button.create"|trans() }}
    </a>
{% endblock %}
