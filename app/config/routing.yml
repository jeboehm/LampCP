jeboehm_lampcp_core:
    resource: "@JeboehmLampcpCoreBundle/Controller/"
    type:     annotation
    prefix:   /

jeboehm_lampcp_config:
    resource: "@JeboehmLampcpConfigBundle/Controller/"
    type:     annotation
    prefix:   /

# Internal routing configuration to handle ESI
#_internal:
#    resource: "@FrameworkBundle/Resources/config/routing/internal.xml"
#    prefix:   /_internal
